# 变更日志

## 2025-12-10

- Excel 导出：改用 ExcelJS，列宽统一缩小 50%，启用自动换行；保留表头加粗、边框与背景色；数据行添加细边框。
- 编号规则：改为 `YYMMNNN`（月度重置 001-999），并发下通过序列表与唯一约束保证不重复；插入遇到唯一冲突自动重试。
- 模板路径：模板加载支持开发与生产环境，若 `fetch` 失败则回退使用本地资源读取（`electronAPI.readFileBuffer`）。
- 发布打包：清理自定义图标与相关脚本，统一使用模板图标；安装器与目录版打包均成功。

